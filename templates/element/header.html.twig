<header>
    {{ include('top-navbar.html.twig', top_navbar) }}
    {{ include('main-navbar.html.twig', main_navbar) }}

    {% if page.tier == 1 %}
        {{  include('tier/1/hero.html.twig', page) }}
    {% elseif page.tier == 2 %}
        {{  include('tier/2/hero.html.twig', page) }}
    {% else %}
        {{ include('breadcrumbs.html.twig', breadcrumbs) }}
    {% endif %}
</header>
