<!DOCTYPE html>
<html lang="en">
	{{> head}}
	<body class="bg-body node-{{page.current.node_id}}">
		<header>
			{{> navbar-top}}
			<div id="window-top">
				<div class="container pt-5 pb-2 mb-2">
					<h1>{{page.current.page_heading}}</h1>
					{{> breadcrumb-trail}}
				</div>
			</div>
		</header>
		<div id="page" class="container">
			<div class="row">
				<!-- left column -->
				<div class="col-sm-12 col-md-8 col-lg-9 bg-4">
					{{#alerts.main}}
					<div class="col-md-12 alert alert-{{type}}">
						<a href="#" class="close" data-dismiss="alert" aria-label="close" title="Close">&times;</a>
						{{value}}
					</div>
					{{/alerts.main}}

<div class="pagepad" style="display: block; text-align: center; font-weight: bold; text-transform: uppercase; font-size: 18px; font-family: Arial, Helvetica, sans-serif; margin-top: 25px;">Authorized Users Only</div>
<h2>Your Account</h2>
{{#user.sign-in.banned}}
<div class="alert alert-danger" role="alert">
	<p>For security reasons, you were banned from the sign-in form. Please wait 30 minutes before trying to sign-in again.</p>
</div>
{{/user.sign-in.banned}}
{{^user.sign-in.banned}}
{{#user.sign-in.failure}}
<div class="alert alert-danger" role="alert">
	<p>Sorry, incorrect email or password.</p>
	<p>For security reasons, after {{user.sign-in.tries.remainder}} more failed login attempts you'll have to wait 30 minutes before trying again.</p>
</div>
{{/user.sign-in.failure}}
<p>Please sign in to access your account.</p>
<div class="col-md-12 bg-1">
	<form method="post" enctype="multipart/form-data" style="max-width: 300px;" name="sign_in">
		<label for="inputEmail">Email</label>
		<input name="sign_in[email]" type="email" id="inputEmail" class="form-control mb-1" placeholder="you@example.org" value="{{user.sign-in.tries.email}}" required autofocus/>
		<label for="inputPassword">Password</label>
		<input name="sign_in[password]" type="password" id="inputPassword" class="form-control mb-1" placeholder="************" required/>
		<div class="text-right">
			<a href="/forgot.html">forgot?</a>
		</div>
		<div class="checkbox">
			<label>
				<input name="sign_in[remember]" type="checkbox" value="remember-me"/> Remember me
			</label>
		</div>
		<button class="btn btn-lg btn-primary" type="submit">Sign in</button>
	</form>
</div>
{{/user.sign-in.banned}}

					<div class="mt-1">
						{{> share-buttons}}
					
					</div>
    			</div>
				{{> navbar-left}}
			</div>
		</div>

		<a href="#0" class="cd-top cd-is-visible cd-fade-out">Top</a>

		<!-- footer -->
		<footer class="footer mt-3 pt-2 pb-2">
			<div class="container">
				<i class="material-icons">copyright</i> {{#f.date}}Y{{/f.date}} {{website.title}}. All rights reserved.
				<a class="block" style="float:right" title="Validate HTML5" href="https://validator.w3.org/nu/?doc={{page.current.alias}}" target="_blank" rel="nofollow">HTML5 <i class="material-icons">check_circle</i></a>
			</div>
		</footer>
		{{> includes}}
	</body>
</html>
<!-- powered by Hoopless -->

